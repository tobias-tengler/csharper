using HotChocolate.Types;

public class ${name} : InputObjectType<${1:Entity}>
{
    protected override void Configure(IInputObjectTypeDescriptor<${1:Entity}> descriptor)
    {
        descriptor.Name("${2:Name}");

        descriptor.Field(i => i.${3:Property}).Type<${0}>();
    }
}